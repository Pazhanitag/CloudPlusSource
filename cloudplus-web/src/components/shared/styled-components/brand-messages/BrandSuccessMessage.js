import styled from 'vue-styled-components';

// Create a <BrandSuccessMessage> Vue component that renders a <div> with success design
const BrandSuccessMessage = styled('div')`
  color: #23d160;
  text-align: center;
  font-size: 0.7rem;
`;

export default BrandSuccessMessage;

