import styled from 'vue-styled-components';

// Create a <BrandErrorMessage> Vue component that renders a <div> with error design
const BrandErrorMessage = styled('div')`
  color: #ff3860;
  text-align: center;
  font-size: 0.7rem;
`;

export default BrandErrorMessage;

